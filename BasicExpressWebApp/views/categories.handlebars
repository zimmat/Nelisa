<div class="container">
  <h2>Category Table</h2>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Category ID</th>
        <th>Category Name</th>
        {{#if is_admin}}<th>Edit</th>
        <th>delete</th>
    <a href="/categories/add">Add a new category</a>
    {{else}}
    <a href=""></a>
    {{/if}}
      </tr>
    </thead>
    <tbody>
      <tr>
        {{#categories}}
        <td>{{category_id}}</td>
        <td>{{category_name}}</td>
        {{#if ../is_admin}}
        <td>
          <a href="/categories/edit/{{category_id}}">Edit</a></td>
        <td>  <a href="/categories/delete/{{category_id}}">Delete</a>
        </td>
        {{else}}
        <td></td>
        {{/if}}
        </tr>
        {{/categories}}
    </tbody>
  </table>
</div>
