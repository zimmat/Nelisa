<div class="container">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>user_id</th>
        <th>username</th>
    <a href="/categories/add">Add user</a>
      </tr>
    </thead>
    <tbody>
      <tr>
        {{#users}}
        <td>{{user_id}}</td>
        <td>{{user_name}}</td>
        <td>{{password}}</td>
          <td><a href="/users/edit/{{user_id}}">Edit</a>
          <a href="/users/delete/{{user_id}}">Delete</a>
        </td>
        </tr>
        {{/users}}
    </tbody>
  </table>
</div>
